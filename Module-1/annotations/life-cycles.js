// Montagem - componente exibido em tela pela primeira vez
//componentDidMount() {};

// Atualizações - Decide se deve renderizar o componente ou não
//shouldComponentUpdate( nextProps, nextState ) {};

// Atualizações - Serve para coletar informações antes de o componente atualizar em tela
//getSnapshotBeforeUpdate( prevProps, prevState ) {};

// Atualizações - Tem acesso as propriedades antigas, e ao snapshot
//componentDidUpdate( prevProps, prevState, snapshot ) {};

// Desmontagem - É executado antes de o componente deixar de existir
//componentWillMount() {};

//Error - Toda vez que ocorrer um erro, ele cai nessa função
//componentDidCatch( error, info ) {};

// Montagem/Atualização - Serve para montar o estado baseado nas informaçãoes das props
//static getDerivedStateFromProps( nextProps, prevState ) {};